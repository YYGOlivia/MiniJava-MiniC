test72 {
  // No error: Initialize a record-type variable containing two Boolean fields with a Boolean sequence value of size 2
  // Extracting the first field of the record in a conditional statement
struct B { boolean a; int n; } p = { true, 1 } ;
   struct B { boolean a; int n; } p =  { true, 1 } ;
   if (p.a) {
   }
}